//const ipcRenderer = require('electron').ipcRenderer;

var $ = global.jQuery = require('jquery');

$(() => 
{
	/*
	$('#openDialog').on('click', e =>
	{
		ipcRenderer.send('show-select-dir-dialog');
	});
	*/

});





















